---
- hosts: 10.0.0.5
  become: yes
  tasks:
  - name: i want to excute direct linux command
    command: hostname -f
    register: myfirsttaskresult
  - name: i wnat to debug first task result
    debug:
       msg: "{{myfirsttaskresult}}"
       